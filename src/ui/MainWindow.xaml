<Window
    x:Class="HoDoKuNative.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Title="HoDoKu Native - High Performance Modern Sudoku">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Command Bar -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton x:Name="NewPuzzleButton" Icon="Add" Label="New Puzzle" Click="OnNewPuzzleClicked" />
            <AppBarButton x:Name="HintButton" Icon="Help" Label="Hint / Step" Click="OnHintClicked" />
            <AppBarButton x:Name="SolveButton" Icon="Play" Label="Solve" Click="OnSolveClicked" />
            <AppBarSeparator />
            <AppBarButton x:Name="ResetButton" Icon="Refresh" Label="Reset" Click="OnResetClicked" />
            <AppBarButton x:Name="ClearButton" Icon="Clear" Label="Clear" Click="OnClearClicked" />
        </CommandBar>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="400" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="380" />
            </Grid.ColumnDefinitions>

            <!-- Left: Interactive Win2D Canvas Sudoku Grid -->
            <Border Grid.Column="0" 
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    Padding="8">
                <canvas:CanvasControl x:Name="SudokuCanvas"
                                      CreateResources="OnCanvasCreateResources"
                                      Draw="OnCanvasDraw"
                                      PointerPressed="OnCanvasPointerPressed"
                                      PointerMoved="OnCanvasPointerMoved"
                                      PointerReleased="OnCanvasPointerReleased"
                                      SizeChanged="OnCanvasSizeChanged" />
            </Border>

            <!-- Right: Analysis Sidebar & Numpad Controls -->
            <Grid Grid.Column="2" RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Status & Difficulty Card -->
                <StackPanel Grid.Row="0" Spacing="4">
                    <TextBlock Text="Puzzle Analysis" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <TextBlock x:Name="PuzzleStatusText" Text="Ready (81 cells free)" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>

                <!-- Deduction Step Explorer -->
                <ListView x:Name="StepListView" 
                          Grid.Row="1" 
                          SelectionMode="Single"
                          SelectionChanged="OnStepSelectionChanged"
                          Header="Deduction Steps"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          CornerRadius="6">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Padding="8" Spacing="2">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Explanation}" TextWrapping="Wrap" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Numpad Palette -->
                <Grid Grid.Row="2" ColumnSpacing="6" RowSpacing="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="42" />
                        <RowDefinition Height="42" />
                        <RowDefinition Height="42" />
                        <RowDefinition Height="42" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Grid.Column="0" Content="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnDigitClicked" Tag="1" />
                    <Button Grid.Row="0" Grid.Column="1" Content="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnDigitClicked" Tag="2" />
                    <Button Grid.Row="0" Grid.Column="2" Content="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnDigitClicked" Tag="3" />

                    <Button Grid.Row="1" Grid.Column="0" Content="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnDigitClicked" Tag="4" />
                    <Button Grid.Row="1" Grid.Column="1" Content="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnDigitClicked" Tag="5" />
                    <Button Grid.Row="1" Grid.Column="2" Content="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnDigitClicked" Tag="6" />

                    <Button Grid.Row="2" Grid.Column="0" Content="7" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnDigitClicked" Tag="7" />
                    <Button Grid.Row="2" Grid.Column="1" Content="8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnDigitClicked" Tag="8" />
                    <Button Grid.Row="2" Grid.Column="2" Content="9" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnDigitClicked" Tag="9" />

                    <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Content="Erase Candidate / Value" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="OnEraseClicked" />
                </Grid>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}" Padding="12,6">
            <TextBlock x:Name="StatusBarText" Text="HoDoKu Native Engine Active (C++20, DLX Solver, Zero WinRT in Core)" Style="{ThemeResource CaptionTextBlockStyle}" />
        </Border>
    </Grid>
</Window>

